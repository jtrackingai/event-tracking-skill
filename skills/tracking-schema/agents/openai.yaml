interface:
  display_name: "Tracking Schema"
  short_description: "Design, validate, and confirm event schema"
  default_prompt: "Use $tracking-schema to prepare, validate, and confirm the event schema for this tracking run."

policy:
  allow_implicit_invocation: false
